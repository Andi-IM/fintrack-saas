# Secret key used for authenticating API requests (e.g. Bearer Token or X-API-Key header)
# Ensure this matches the key configured on the frontend calling this service.
OCR_API_KEY=your_secure_random_key_here
