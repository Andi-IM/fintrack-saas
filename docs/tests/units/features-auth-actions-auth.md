# Test Case Document: Auth Actions

## Test Cases

| ID Test | Deskripsi | Langkah-langkah Pengujian | Data yang Diuji | Ekspektasi Hasil | Realita Hasil | Status |
|---------|-----------|---------------------------|-----------------|------------------|---------------|--------|
| TC-AUTH-001 | DefaultOriginResolver.resolve mengembalikan APP_URL ketika ada | 1. Set process.env.APP_URL = 'https://my-app.com'<br>2. Create DefaultOriginResolver<br>3. Panggil resolve() | APP_URL set | - Mengembalikan APP_URL value | Sesuai ekspektasi | Lulus |
| TC-AUTH-002 | DefaultOriginResolver.resolve mengembalikan VERCEL_URL ketika ada dan APP_URL tidak ada | 1. Hapus APP_URL dari env<br>2. Set NEXT_PUBLIC_VERCEL_URL = 'my-vercel.vercel.app'<br>3. Create DefaultOriginResolver<br>4. Panggil resolve() | VERCEL_URL set, APP_URL not set | - Mengembalikan 'https://my-vercel.vercel.app' | Sesuai ekspektasi | Lulus |
| TC-AUTH-003 | DefaultOriginResolver.resolve fallback ke x-forwarded-host header | 1. Hapus APP_URL dan VERCEL_URL dari env<br>2. Mock headers untuk x-forwarded-host dan x-forwarded-proto<br>3. Create DefaultOriginResolver<br>4. Panggil resolve() | headers with x-forwarded-* | - Mengembalikan 'https://x-host.com' | Sesuai ekspektasi | Lulus |
| TC-AUTH-004 | DefaultOriginResolver.resolve fallback ke host header dan localhost logic | 1. Hapus APP_URL dan VERCEL_URL dari env<br>2. Mock headers untuk host = 'localhost:3000'<br>3. Create DefaultOriginResolver<br>4. Panggil resolve() | host header: localhost:3000 | - Mengembalikan 'http://localhost:3000' | Sesuai ekspektasi | Lulus |
| TC-AUTH-005 | resolveAuthOrigin mengembalikan origin dengan sukses | 1. Set APP_URL = 'https://env-app.com'<br>2. Panggil resolveAuthOrigin() | APP_URL set | - Mengembalikan APP_URL value | Sesuai ekspektasi | Lulus |
| TC-AUTH-006 | login melakukan sign in dengan OAuth dan redirect ke url provider OAuth | 1. Mock supabase.auth.signInWithOAuth return url<br>2. Mock resolver resolve origin<br>3. Panggil login(mockResolver) | mock resolver and signInWithOAuth | - signInWithOAuth dipanggil dengan provider github dan redirectTo<br>- redirect dipanggil dengan oauth url | Sesuai ekspektasi | Lulus |
| TC-AUTH-007 | login redirect kembali ke halaman login saat terjadi error inisiasi OAuth | 1. Mock supabase.auth.signInWithOAuth return error<br>2. Mock resolver resolve origin<br>3. Panggil login(mockResolver) | mock resolver and signInWithOAuth returns error | - redirect dipanggil ke /login dengan message parameter | Sesuai ekspektasi | Lulus |
| TC-AUTH-008 | logout memanggil signOut dan redirect ke halaman login | 1. Mock supabase.auth.signOut return sukses<br>2. Panggil logout() | mock signOut success | - signOut dipanggil 1x<br>- redirect dipanggil ke /login | Sesuai ekspektasi | Lulus |
